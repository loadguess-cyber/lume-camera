# 이 저장소에서 일할 때

## 작업을 끝낼 때는 반드시 눌러볼 수 있는 링크를 준다

무언가를 만들거나 고쳤으면, 답변 맨 끝에 **바로 열리는 링크**를 붙인다.
"커밋했습니다"로 끝내지 않는다 — 눌러서 확인할 수 없으면 끝난 게 아니다.

- **웹 화면을 만들었거나 고쳤으면** 실제로 동작하는 링크를 먼저 준다.
  이 저장소의 앱들은 빌드 도구가 없는 순수 웹이라, 파일 하나로 묶어
  아티팩트로 올리면 폰에서 바로 열린다. (`guin-caster` 는 `node scripts/bundle.js`)
  같은 것을 고칠 때는 **같은 파일 경로로 다시 올려 링크를 그대로 유지**한다.
- **코드만 바뀌었으면** 해당 브랜치나 파일의 GitHub 링크를 준다.
- 링크가 여러 개면 눌러볼 것을 맨 위에 둔다.

## 프로젝트

| 폴더 | 무엇 |
|---|---|
| `www/`, `scripts/` (저장소 루트) | **LUME** — 라이트룸 프리셋을 실시간으로 입히는 카메라 |
| `guin-caster/` | **구인 캐스터** — 구인광고를 채널별 문안으로 바꾸고 게재·끌어올리기를 관리 |

각 폴더의 README 에 자세한 내용이 있다. 두 프로젝트는 서로 의존하지 않는다.
